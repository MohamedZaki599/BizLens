/**
 * AssistantDrawer — removed fake chat interface.
 * The assistant now lives exclusively at /app/assistant as a deterministic digest.
 * This component is kept as a no-op export to avoid breaking imports.
 */
export const AssistantDrawer = () => null;
